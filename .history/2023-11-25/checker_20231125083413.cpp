#include "testlib.h"

using namespace std;

int arr[1000005];

int main(int argc, char* argv[]) {
    registerTestlibCmd(argc, argv);
	int T = inf.readInt();
	rep (__, 1, T) {
		int n = inf.readInt(), m = inf.readInt();
		rep (i, 2, n) {

		}
	}
}